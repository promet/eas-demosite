drupal:
    settings:
        db_url: "mysql://root:pass@localhost/default"
        databases:
            default:
                default:
                    database: testDB
                    username: testDBA
                    password: testPASS
                    driver: mysql
                    host: localhost
    ini: {}
    include:
        require_once: ["$DRUPAL_ROOT/sites/default/vendor/autoload.php"]
